
class Solution:

    def rotate(self, matrix: []):
        row = len(matrix)
        col = row

        # Transpose the matrix

        # Swap the values of each row


image = [[1, 2], [3, 4]]

solution = Solution()

solution.rotate(image)
